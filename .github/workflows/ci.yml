name: JFA Unify Backend CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'backend/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'backend/**'

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: jfa_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt -r requirements-test.txt

      - name: Lint with ruff
        run: |
          cd backend
          ruff check .

      - name: Type check with mypy (if configured)
        run: |
          cd backend
          mypy app --ignore-missing-imports 2>/dev/null || true

      - name: Run pytest with coverage + gate
        # -m "not integration": exclui os testes RLS que exigem Postgres real com
        #   roles app_user + políticas RLS (TESTING=1 usa SQLite — RLS não existe).
        #   Esses correm num job de integração dedicado (docker-compose.test.yml).
        # --cov-fail-under=50: baseline unit medido ~55%; gate a 50 (margem ~5pp).
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/jfa_test
          TESTING: '1'
        run: |
          cd backend
          pytest tests/ -m "not integration" \
            --cov=app \
            --cov-report=xml \
            --cov-report=term-missing \
            --cov-fail-under=50 \
            --junitxml=test-results.xml \
            -v

      - name: Upload coverage to Codecov
        if: always()
        uses: codecov/codecov-action@v3
        with:
          files: ./backend/coverage.xml
          flags: unittests
          name: codecov-umbrella

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: backend/test-results.xml
